/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NumberBetweenValidatorDirective } from './number-between-validator.directive';

describe('NumberBetweenValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberBetweenValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
